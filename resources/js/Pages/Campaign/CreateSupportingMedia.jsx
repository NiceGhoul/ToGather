const supportingMedia = () => {
    return(
        <div>
            <Button onClick={() => window.history.back()}> Back </Button>
            <h1>preview page for create new campaign</h1>
        </div>
    )
}
